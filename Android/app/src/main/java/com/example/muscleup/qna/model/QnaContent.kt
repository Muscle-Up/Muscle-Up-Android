package com.example.muscleup.qna.model

data class QnaContent(val postId : Int,
                      val title : String,
                      val writer : String,
                      val createdAt : String)