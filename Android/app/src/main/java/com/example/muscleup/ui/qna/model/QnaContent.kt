package com.example.muscleup.ui.qna.model

data class QnaContent(val postId : Int,
                      val title : String,
                      val writer : String,
                      val createdAt : String)