<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/radius_greet">

    <TextView
        android:id="@+id/dialog_tv_message"
        android:layout_width="0dp"
        android:layout_height="84dp"
        android:gravity="center"
        android:textColor="@android:color/black"
        android:textSize="18sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/dialog_btn_ok"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:background="@android:color/white"
        android:text="네"
        android:textColor="@android:color/black"
        android:textSize="15sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/dialog_btn_no"
        app:layout_constraintTop_toBottomOf="@id/dialog_tv_message" />

    <Button
        android:id="@+id/dialog_btn_no"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:background="@android:color/white"
        android:text="아니요"
        android:textColor="@android:color/black"
        android:textSize="15sp"
        app:layout_constraintLeft_toRightOf="@id/dialog_btn_ok"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/dialog_tv_message" />

</androidx.constraintlayout.widget.ConstraintLayout>